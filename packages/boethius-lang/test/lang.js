import {expect} from "chai";
import lang from "../lang";

describe("parser", () => {
    describe("notes", () => {
        it("should fail", () => {
            expect(1).to.equal(2);
        });
    });
});
